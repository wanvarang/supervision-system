# Claude Code Instructions

## Git Workflow

- Always push directly to `main` branch — never create feature branches or PRs
- Remote is configured with GitHub token; use: `git push -u origin main`
- Token already embedded in remote URL (no extra auth needed)
